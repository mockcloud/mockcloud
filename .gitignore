# Dependencies
node_modules/
ui/node_modules/

# UI build output (Docker builds this, don't commit)
ui/dist/

# Environment / secrets
.env
.env.*

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# MockCloud runtime data (if any state gets written to disk)
data/
*.db

# Go build output (scripts/conformance.mjs builds here) — but keep the npm
# launcher shim (bin/mockcloud.js) tracked.
/bin/mockcloud
/bin/mockcloud.exe
/bin/mockcloud-*
/bin/@mockcloud
